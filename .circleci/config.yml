version: 2.1
jobs:
  pr_check:
    docker:
      - image: node:11.10.1

    steps:
      - attach_workspace:
          at: .
      - aws-cli/setup:
          profile-name: default
          aws-access-key-id: AWS_KEY
          aws-secret-access-key: AWS_SECRET
          aws-region: AWS_REGION

      - checkout

      - run: npm install
      # - run: apt-get install mocha
      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "package.json" }}

      # run tests!
      - run:
          name: "Build"
          command: node ciserver.js

      - run:
          name: "Test"
          command: |
            npm run test
            echo 'Done'
  build:
    docker:
      - image: node:11.10.1
    executor: aws-cli/default
    steps:
      - attach_workspace:
          at: .
      - aws-cli/setup:
          profile-name: default
          aws-access-key-id: AWS_KEY
          aws-secret-access-key: AWS_SECRET
          aws-region: AWS_REGION

      - checkout

      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "package.json" }}
            # fallback to using the latest cache if no exact match is found
            - v1-dependencies-

      - run: npm install
      # run tests!
      - run:
          name: "Test"
          command: node ciserver.js
          # background: true
      - run:
          name: "Test"
          command: npm run test
      - run:
          command: ls

      - run:
          name: "Upload to S3"
          command: |
            aws deploy push \
              --application-name csye6225-webapp \
              --description "This is a revision for the application Webapp" \
              --ignore-hidden-files \
              --s3-location s3://codedeploy.shaktichetan.me/artifacts.zip \
              --source . \

  deploy:
    executor: aws-cli/default
    steps:
      - attach_workspace:
          at: .
      - aws-cli/setup:
          profile-name: default
          aws-access-key-id: AWS_KEY
          aws-secret-access-key: AWS_SECRET
          aws-region: AWS_REGION
      - run:
          name: CodeDeploy
          command: aws deploy create-deployment --application-name csye6225-webapp --s3-location bucket=codedeploy.shaktichetan.me,key=artifacts.zip,bundleType=zip --deployment-group-name csye6225-webapp-deployment

orbs:
  aws-cli: circleci/aws-cli@0.1.22

workflows:
  version: 2
  pr_check_workflow:
    jobs:
      - build
      - deploy:
          requires:
            - build
  # build_deploy:
  #   jobs:
  #     - build:
  #         filters:
  #           branches:
  #             only: master
